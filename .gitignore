# macOS specific files
.DS_store

# Output of CMAKE_EXPORT_COMPILE_COMMANDS is often symlinked into the project's root directory
/compile_commands.json

# Nested build directory
/src/build*
/src/o-mvll-build_ndk_*

# Build directory
build

# clangd cache folder
.cache

*.env

# python cache folders for tests
__pycache__

# tmp folder for deps generation
tmp

# dist folder tmp contents
dist/omvll_ndk_r25c.so
dist/omvll_xcode_14.dylib
dist/Python-3.10.7

# --- fork local additions ---

# AI-assisted docs / personal learning notes (not upstreamed)
CLAUDE.md
learn.md
learn.html
image.png
image-*.png

# Editor / IDE / venv
.vscode/
.venv/
.idea/

# Lab build artifacts (.so 产物 + IDA 数据库)
labs/**/*.so
labs/**/*.id[0-9]
labs/**/*.nam
labs/**/*.til

# O-MVLL 运行时产物
omvll-logs/
omvll-tmp/

# 分发 tarball 与解包目录（几十 MB，不要放进 git）
omvll_v1-6-0_*.tar.gz
omvll_v1-6-0_*/
